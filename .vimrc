" Hiển thị số dòng
set number

" Hiển thị số dòng tương đối (hữu ích khi di chuyển bằng số dòng)
" set relativenumber

" Tô sáng dòng hiện tại
" set cursorline

" Tô màu cú pháp
syntax on

" Tự động thụt dòng
set autoindent
set smartindent

" Hiển thị dòng lệnh
set showcmd

" Hiển thị chế độ (INSERT, NORMAL)
set showmode

" Chỉ ra vị trí con trỏ (dòng, cột)
set ruler

" Bật tìm kiếm highlight
set hlsearch
set incsearch

" Không wrap dòng dài
set nowrap

" Mã hóa UTF-8
set encoding=utf-8
set fileencodings=utf-8

" Màu nền tối
set background=dark

